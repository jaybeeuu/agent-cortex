# Testing Patterns

Shared testing defaults:

- Start with a failing test (red), then implement minimal code (green).
- Keep tests deterministic and behavior-focused.
- Assert public contracts, not internal implementation details.
- Prefer small, focused test slices per behavior.
