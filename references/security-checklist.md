# Security Checklist

Before finalizing changes:

- Scan diffs for secrets (tokens, keys, passwords, credentials).
- Avoid logging sensitive user or system data.
- Validate unknown input at boundaries.
- Use least-privilege defaults for commands and automation.
