# Skill Anatomy

Baseline structure for skills in this repo:

- `SKILL.md` with YAML front-matter (`name`, `description`).
- Focused workflow steps that are directly executable.
- Optional `scripts/` for deterministic helper automation.
- Optional `REFERENCE.md` for overflow details.
